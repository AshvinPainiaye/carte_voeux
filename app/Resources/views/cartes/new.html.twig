{% extends 'base.html.twig' %}

{% block body %}

    <style>


        input[type="radio"]{
            margin-right: 10px;
        }

        input[type="radio"] + label{
            position: relative;
            display: inline-block;
            padding-right: 30px;
            margin-bottom: 0;
            font-weight: 400;
            vertical-align: middle;
            cursor: pointer;
        }


    </style>

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 class="text-center">Cartes creation</h1>
            </div>
        </div>

        <img src="{{asset('images/')~ image.imageName }}" class="img-responsive center-block">

        <div class="row">
            <div class="col-md-12">

                {{ form_start(form) }}

                <div class="form-group">
                    {{ form_label(form.message) }}
                    {{ form_widget(form.message, {'attr': {'class':'form-control'}}) }}
                    {{ form_errors(form.message) }}
                </div>

                <input type="submit" value="Crée" class="btn btn-success"/>

                {{ form_end(form) }}

            </div>
        </div>
    </div>


{% endblock %}

